# 下摄像头实时流修复验证 20260805-131600

## 结果

- 开始诊断时飞机仍在空中，因此只读取状态，没有重启任何服务。
- 飞机落地并确认 `STANDBY`、`armed=false`、高度 0、无控制进程后才开始恢复操作。
- 吊舱可见光 RTSP 已恢复；`ffprobe` 识别为 H.264、1280x720、25 fps。
- 官方 SDK 示例 `06_pod_frame_acquire` 连续保存 3 张实时帧，成功 3 次、超时 0 次。
- 三张照片均为当前实时画面，清晰且内容略有变化，排除了旧文件缓存。
- 最终再次确认飞机保持在地面待机状态，修复过程中未发送飞行命令。

## 故障定位与恢复

1. 修复前，直接访问 `rtsp://192.168.1.120:8554/arRtspStream` 超时；系统日志持续报告 `ov48@rtsp error` 并周期性重启媒体工作进程。
2. 只重启 Jetson 侧 `ov48` 媒体工作进程后，守护进程能正常拉起新进程，但 RTSP 仍然超时，说明故障不只在本地接收端。
3. 根据吊舱官方 OTA 页面使用 `POST http://192.168.1.120:8080/restart` 重启吊舱设备。
4. 吊舱恢复后，RTSP 探测和 SDK 取帧均成功。

## 尚未解决

调用官方示例中的 `gimbalDownSet` 返回 `Ok(0)`，但照片仍拍到天花板、格栅与灯具。实时数据链路已经修好，当前剩余问题是云台方向映射、安装姿态或“向下”预置位定义不符合预期。下一次应在地面无桨或仿真条件下逐个验证云台角度接口，再进行场地图像采集。
