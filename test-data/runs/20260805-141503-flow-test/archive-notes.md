# 恢复归档说明

- 类型：信号收尾修复前的受控 10 秒 dry-run。
- 程序仅连接 SDK、订阅裁判事件和遥测；没有收到裁判事件。
- `commands.jsonl` 与 `events.jsonl` 均为空，没有飞控、云台或夹爪写命令。
- 旧启动脚本被外部 `timeout` 终止后，没有执行生成服务器 `SHA256SUMS` 的收尾步骤。
- 本地下载后逐文件与服务器即时 SHA-256 对比，结果记录在 `RECOVERED_SHA256SUMS`；补充说明和恢复清单再由 `ARCHIVE_SHA256SUMS` 一并校验。
- 该缺陷已在后续 `20260805-143310` dry-run 中修复并验证。
