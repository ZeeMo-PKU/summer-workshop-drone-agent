# 下摄云台标定 20260805-153042

- 飞机处于仿真、落地待机和无并行控制器状态。
- `PodVisibleLight` 四次开流及取帧均成功，四张照片均为 1280x720。
- `center`、`down`、`pitch-negative`、`pitch-positive` 四张画面基本相同，均拍到天花板。
- 现场随后确认无人机实物为倒放状态，因此天花板方向对应相对机体的下方；采用官方 `gimbalDownSet` 对应的 `down` 作为仿真预置位。
- 本次标定后吊舱发生过热离线，后续程序改为拍照时按需开流并在取帧后立即关闭。
