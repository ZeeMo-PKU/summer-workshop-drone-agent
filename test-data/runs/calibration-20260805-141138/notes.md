# 云台地面校准结果

- 飞控参数 `CFG_FLIGHTSIM=1`，确认处于仿真模式。
- 校准前状态为 `STANDBY`、`armed=false`、`sdk_mode=true`、高度 `0 m`，没有其他比赛控制进程。
- 依次测试 `center`、官方 `down`、俯仰 `-90°` 和俯仰 `+90°`。
- 四张 `PodVisibleLight` 照片均为相同的正前方百叶窗视角，没有任何一张明确朝向地面。
- 本次只移动仿真云台并抓图，没有发送起飞、位置、返航或夹爪命令。
- 按隔离流程验收规则，完整仿真飞行测试保持阻塞，直至找到有效的下视相机通道或姿态。
