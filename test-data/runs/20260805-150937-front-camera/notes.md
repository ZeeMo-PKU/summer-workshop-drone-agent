# 前置摄像头单帧测试 20260805-150937

## 结果

- 拍照前确认 `CFG_FLIGHTSIM=1`，飞机处于 `STANDBY`、`armed=false`、高度 0、SDK 模式开启，且没有其他控制程序。
- 使用 SDK 官方示例 `08_front_camera_frame_acquire` 和 `StreamChannelType::Front` 获取一帧。
- SDK 成功返回一张 1280x720、BGR 类型的 JPEG，日志统计为 `ok=1`、`timeouts=0`、`saved=1`。
- 服务器与本地照片 SHA-256 一致：`05eab8feff08ab54ea3acf5c00c4155849707afc13292de0bcbe835ec47db126`。
- 本次仅访问相机，没有发送飞行、云台或夹爪命令。
- 拍照程序退出后曾读取到一次 `LANDING.BEGIN_RETURN_LAND`，但当时仍为未解锁、高度 0、速度 0，且没有控制进程；随后连续三次读取均恢复为 `STANDBY`，判断为仿真状态机的短暂过渡。

## 画面观察

- 画面方向相对正常观察方向有明显旋转。
- 中央灯带严重过曝，场地细节无法辨认。
- 画面主要是机体附近的线缆、容器和桌面，前置镜头当前没有形成可用于题目识别的场地视野。
- 画面未发现可识别人脸、账号、密钥或二维码，可纳入公开测试资料。

## 监控页面诊断

- 浏览器地址 `http://127.0.0.1:18080/` 当时没有监听服务，因此页面无法显示。
- 现存 Node 进程监听 `18082` 和 `18083`；`18082` 是裁判网页，`18083` 是区域节点接口。
- 该进程的父启动命令明确设置 `DRONE_PROVIDER=sim`、`REFEREE_HTTP_PORT=18082` 和 `AREA_NODE_PORT=18083`，所以它是纯仿真实例，不是 `config/student-practice.json` 中配置的 18080 真实无人机连接实例。

## 后续建议

- 先确认前置镜头的安装方向、遮挡和照明，再做题目识别测试。
- 若要监控无人机服务器上的仿真飞控数据，应按学生练习脚本启动 `DRONE_PROVIDER=real` 的 18080 实例；不要把当前 18082 纯仿真实例误认为无人机遥测页面。
