# 测试记录 20260805-104410

## 目的

使用服务器 `/opt/iking/match_agent/match.cpp` 的最新源码编译临时二进制，执行一轮完整比赛路径，验证飞行、双相机取帧、夹爪和异常返航逻辑。

## 结果

- `MATCH_STARTED` 后起飞成功，并以绝对位置模式到达 B 场景触发区。
- 收到题目 `REAL-4-02`，裁判给出的正确答案为 B；当前程序没有题目识别逻辑，仍按硬编码目标 A 执行。
- 前视通道 `Front` 取帧成功，生成 `round_1_scene_B.jpg`。
- 到达答题区 B 观察点后，下摄通道 `PodVisibleLight` 重新连接，但 `acquireFrame` 返回 `status=-1`，没有生成本轮布局照片。
- 程序继续飞到物理答题位 A，`gripperOpen` 返回 `status=-1`，错误为 `gimbalControlAngleSetFailed`。
- 夹爪失败后保护逻辑生效，飞机返回起点并悬停。
- 终止测试进程后自动返航落地；SSH 最终确认 `STANDBY`、`armed=false`、高度 0、速度 0。
- 监控平台最终显示 100 分，该分数来自 B 场景触发；本轮没有完成投球答题。

## 公开数据说明

本轮前视原图包含现场人员和环境，因此未提交到公开仓库。原图已保存在本地私有测试归档；其 SHA-256 和文件大小记录在 `metadata.txt` 中。仓库保留完整程序日志，可复现本轮控制流程和故障点。
