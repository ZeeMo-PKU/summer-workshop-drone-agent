# 20260806-103235 修复前仿真运行

该目录保存服务器 `/opt/iking/match_agent_flow_test/runs/20260806-103235` 的完整原始数据副本。本次运行用于定位问题，不代表修复后的飞行验收结果。

项目负责人已明确授权本次照片用于公开项目报告；照片仍应仅用于本项目测试与复盘。

## 结果摘要

- 第 1 至第 4 轮完成起飞、A/B 触发区、两路拍照、投放、返航和降落，但裁判均判定投放错误。
- 语义答案和布局最终使用显式仿真真值。Qwen 的四次布局识别均为 `INVALID`；语义识别三轮为 `INVALID`，一轮返回 `B` 但平台真值为 `A`。
- 原始前摄照片只包含室内人员和设备，原始下摄照片只包含天花板，没有裁判网页中的虚拟题图或 A/B/C 布局，因此不能作为真实视觉识别通过。
- 程序先在物理位 2 拍摄布局。吊舱改变姿态后，裁判桥停止更新闭合夹爪位置，最终把实际投放误记为物理位 2。
- 投放后额外飞回启动点耗时约 8 至 11 秒；之后 SDK `returnToHome` 至 `STANDBY` 约 40 至 41 秒。
- 第 5 轮起飞时收到 `MATCH_FINISHED`。旧逻辑在 `TAKEOFF` 阶段只发送一次返航命令，飞控随后停在 `POSITION`，最终由独立恢复程序完成降落。

## 对应修复

- 下摄识别后重新闭合夹爪，再飞往目标位。
- 本地裁判桥在夹爪状态暂时未知时继续更新最后闭合位置。
- 投放后直接调用 `returnToHome`，删除重复的 `setPosition(start)`。
- 返航状态机在 `TAKEOFF` 时等待，在 `POSITION/MISSION` 时按间隔重试，直到确认 `STANDBY`。
- Qwen 提示词明确拒绝用无关房间、人员、天花板或空画面推断答案。

原始文件位于 `raw/`，完整性校验见 `SHA256SUMS`。
